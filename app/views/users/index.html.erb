<div class="container">
  <div class="col-xs-12">
    <div class="search">
    <%= form_tag users_path, method: "get" do %>
      <div class="input-group">
        <%= text_field_tag :search, params[:search], placeholder: 'Search through our users', class: 'searchTerm', remote: true %>
        <button  type="submit" class="searchButton">
          <i class="fa fa-search"></i>
        </button>
      </div>
    <% end %>

      <!-- <form action="index">
        <input type="text" class="searchTerm" placeholder="Search through our users" name="search">
        <button  type="submit" class="searchButton">
          <i class="fa fa-search"></i>
        </button>
      </form> -->
    </div>
  </div>

  <div class="row">
    <ul>
      <% @users.each do |user| %>
      <%= render "users/user_card", user: user %>
      <% end %>
    </ul>
  </div>
</div>
