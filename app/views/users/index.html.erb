<div class="container">
  <div class="col-xs-12">
    <div class="search">
    <%= form_tag users_path, method: "get" do %>
      <div class="input-group">
        <%= text_field_tag :search, params[:search], placeholder: "What educational and professional skills are you looking for?", class: 'searchTerm', remote: true %>
        <button  type="submit" class="btn btn-info searchButton">
          <i class="fa fa-search"></i>
        </button>
      </div>
    <% end %>
    <br>

      <!-- <form action="index">
        <input type="text" class="searchTerm" placeholder="Search through our users" name="search">
        <button  type="submit" class="searchButton">
          <i class="fa fa-search"></i>
        </button>
      </form> -->
    </div>
  </div>
  <hr>
<div class="container text-center">
     <h2 ><FONT FACE="Impact" SIZE="6" COLOR="#22395b">Change Makers</FONT>-<FONT FACE="Impact" SIZE="6" COLOR="#FF715B"> <%= @users.count  %> </FONT>-</h2>
  <div class="row">
    <ul>
      <% @users.each do |user| %>
      <%= render "users/user_card", user: user %>
      <% end %>
    </ul>
  </div>
</div>
</div>
